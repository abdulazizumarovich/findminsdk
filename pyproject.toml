[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "findminsdk"
version = "0.1.0"
description = "Analyze Android dependencies for minSdk compatibility and lock versions"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
findminsdk = "findminsdk:main"
